# AGENTS.md

## Cursor Cloud specific instructions

This repository (`district`) is currently an empty/newly initialized project with no source code, dependencies, or services.

- **State**: Single `README.md` file, no application code, no package manager lockfiles, no build system.
- **Dependencies**: None to install.
- **Services**: None to run.
- **Tests**: None configured.
- **Lint**: None configured.

When code is added to this repository, update this section with relevant development instructions (how to install dependencies, run services, lint, test, build, etc.).
